#include <stdio.h>

typedef struct 
{
    int length;
    char *buf;
}subsample;


typedef struct 
{
    int numstruct;
    subsample *st;
}sample;

int main()
{
    subsample *subptr;
    subptr=(subsample*)malloc(sizeof(subsample);
     
    return 0;
}
